{
  "name": "@spotme/ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "React 19 + TypeScript client surfaces. Domain shells (discovery, exchange, events, moments, assistant) as a LIBRARY, not an app: no router, no auth, no backend, no deployable entry. Consumed by apps/web through the island host. Phase 2-6 surfaces remain dark and unmounted.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "test:unit": "vitest run",
    "test": "npm run check:boundaries && npm run typecheck && npm run test:unit"
  },
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.2",
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^30.0.1",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "typescript": "^5.6.3",
    "vite": "^5.4.9",
    "vitest": "^4.1.10"
  }
}
