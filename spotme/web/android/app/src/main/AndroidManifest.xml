<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      allowBackup was "true" with no exclusion rules, which is the default-unsafe
      configuration: Android Auto Backup then uploads the app's data directory to
      the user's Google Drive, and for a Capacitor app that directory is
      app_webview/Local Storage — every message, every photo data URL (including
      view-once photos that were never opened), every room secret, and the server
      auth tokens. All of it outside the end-to-end boundary, restorable by anyone
      who holds the Google account rather than the phone.

      "false" is the right default for an encrypted messenger; Signal and WhatsApp
      both handle backup explicitly instead of letting the platform do it. The
      dataExtractionRules file closes the Android 12+ device-transfer path, which
      allowBackup alone does not govern.
    -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- getUserMedia inside the WebView: voice notes, photo capture, calls. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- The alert chime's haptic. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Android 13+ requires this explicitly, or Notification.requestPermission()
         silently never prompts. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
</manifest>
