# PR #143 shipped — merge, deploy, live verification

**Date:** 2026-08-07 · **Merge commit:** `e304d27` · **Result: LIVE on both halves.**

## 1. Pre-merge diff audit — clean

Eight commits ahead of master, nothing accumulated from #136 or elsewhere:

```
92280e0  feat(moments): drop `public` from the composer picker (A4)
9eff7ed  docs(report): A5 answered
7bdc3fe  test(moments): fix the audience spec's setup
ea10957  docs(report): Moments audience + interaction layer
c4f11eb  feat(moments): optimistic like burst (B1) + single playback owner (B6)
0ce2d81  fix(moments): the author never saw their own posts
81a0028  fix(storage): surface the real IndexedDB failure
b52ef00  docs(report): Moments video upload diagnosis
```

19 files, **+1904 / −85**. Expected set exactly: storage fix, audience fix,
B1/B6, three reports, and the test corrections. No STOP condition.

## 2. Merge

CI green on all four before merging — web, backend against Postgres, e2e,
compose. Undrafted, merged as `e304d27`.

## 3. Web — promoted, not merely built

```
state   target      sha       ref     githubDeployment
READY   production  e304d27b  master  1
```

`target: production` + `githubCommitRef: master` + `githubDeployment: 1` is the
git-wired promotion signature from `CLAUDE.md`, not a manual `--prod` and not a
preview. Created 17:00:32 UTC.

Content-verified in the live bundle (`assets/index-5yAsfjbe.js`) rather than
trusting the green check:

| check | result |
|---|---|
| picker array | ``[`nearby`,`friends`,`private`]`` — **exactly three** |
| `public` badge label retained | yes (`Anyone, beyond your area`) — existing public posts still render |
| `Only you` label present | yes |
| like-burst class `mo-pop` | present |
| audience badge class `mo-aud` | present |

## 4. Backend — deployed from the repo root

`railway up --service api` from `/home/user/fable`, not `spotme/backend`.
Schema-neutral: no migrations on this branch.

```
buildId : f6518ad8aa759607   (was 1963ad89dc892400)
health  : 200
ready   : {"status":"ready","checks":{"db":"up","redis":"up"}}
```

The build id is computed from backend source, so the flip proves the running
process was built from merged master — not that a deployment merely succeeded.

## 5. The audience fix is LIVE — verbatim responses

One fresh adult account, three posts, both feeds read back:

```
create nearby  -> {"id":"cmsj719c400072ilszosq8z4d", ...}
create friends -> {"id":"cmsj71bfx00092ils6ug6kz2l", ...}
create private -> {"id":"cmsj71chd000b2ils9k6mbtk5", ...}

--- nearby feed (author's own view) ---
    private  cmsj71chd000b2ils9k6mbtk5  audience-check private
    friends  cmsj71bfx00092ils6ug6kz2l  audience-check friends
    nearby   cmsj719c400072ilszosq8z4d  audience-check nearby
    -> 3 of my posts visible, 23 total rows

--- friends feed (author's own view) ---
    private  cmsj71chd000b2ils9k6mbtk5  audience-check private
    friends  cmsj71bfx00092ils6ug6kz2l  audience-check friends
    nearby   cmsj719c400072ilszosq8z4d  audience-check nearby
    -> 3 of my posts visible, 3 total rows
```

**The friends feed returned 3 rows and all three are the author's own**, with no
`MomentFollow` row anywhere — the account was created minutes earlier and
follows nobody. Before this change that feed was empty.

### "Visible to its author ONLY" — proved with a second account

The author-side result above is only half the claim. A second fresh account was
created and read the same three posts:

```
stranger sees in nearby : ['nearby']
stranger sees in friends: none of the author's posts
stranger sees in city   : none of the author's posts
```

So the private post is invisible to everyone but its author, the friends post
requires a follow, and the nearby post is still public to the area — **the fix
widened by exactly one person and narrowed nothing.**

### Picker

Three options, confirmed in the shipped bundle (§3): Nearby · Friends · Only
you. `public` remains a valid stored value and still renders its badge.

All probe accounts deleted (`DELETE /api/users/me` → 200).

## Known and unfixed — untouched by this mission, as instructed

- **likes do not reach the author** (B2 not built) — B1 makes the gesture
  instant, not the notification
- **blank screen when IndexedDB is unavailable** — still the most serious open
  item; the app does not degrade there, it fails to start
- @yuv2 has no Moments allowlist row
- `ysnap` is red and is not a Spot Me host

Nothing was merged beyond #143, no allowlist row touched, no flag created or
flipped, no Vercel setting changed, nothing rotated. Environment variables
referenced by name only.
