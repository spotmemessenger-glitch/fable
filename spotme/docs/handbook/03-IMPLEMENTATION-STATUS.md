# 03 — Implementation Status (Six-State Model, G2)

**Every subsystem is in exactly one of six states, with evidence.** This is the
authoritative "what is real" map. Evidence is a merged commit / master file
path or an open PR + branch — **verified against the repository, never against
another document**. Full audit evidence:
[SPOTME-REPO-AUDIT-2026-08-03](../SPOTME-REPO-AUDIT-2026-08-03.md).

> Verified 2026-08-04 against `master` after landing the Platform Phase 2 chain
> (`Merge PR #80`…`#85` — Smart Nearby Discovery Map foundation landed **DARK**:
> `DiscoveryModule` NOT imported by `AppModule`, both crypto flags false, no
> route, no flag flip, no deploy). Prior baseline was `9147485`
> (`Merge pull request #78` — Platform Phase 1). Landing SHAs in the rows below.
> *Implemented (Merged)* is on master and runs. *Implemented (Draft PR)* is
> built but lives only on a branch behind an open, unmerged PR — usually dark —
> and is **not** in the product.

## The six states

| State | Meaning |
|---|---|
| **Implemented (Merged)** | On `origin/master`. Runs (or ships dark on master). |
| **Implemented (Draft PR)** | Built, on a branch, open PR, not merged. Not in the product. |
| **In Progress** | Being worked; no complete/open PR yet. |
| **Planned** | Approved direction but not built. |
| **Deferred** | Deliberately not-now; blocked on a gate or decision. |
| **Retired** | Superseded/withdrawn; kept only as history. |

## Subsystem status

| Subsystem | Status | Evidence (repo-verified) |
|---|---|---|
| Messaging (1-1 chat, knocks, receipts, replay) | Implemented (Merged) | `spotme/web/src/lib/rooms.js`, `reach.js`; `spotme/backend/src/chat/` (gateway + controller) |
| Groups (roles, bans, grants) | Implemented (Merged) | `spotme/backend/src/groups/` (16 routes); `spotme/web/src/lib/groups-api.js`, `group-perms.js` |
| Media (attachments; IndexedDB → bucket) | Implemented (Merged) | PRs #18/#19; `web/src/lib/blobstore.js`, `media-transfer.js`; `backend/src/storage/media.controller.ts` |
| Voice notes | Implemented (Merged) | `web/src/views/chat.js:3904` (MediaRecorder), `web/src/lib/voice.js` |
| View-once media | Implemented (Merged) | Prisma `ViewOnce` model; `web/test/viewonce.test.js`; race fix in PR #20 |
| Disappearing messages | Implemented (Merged) | `msgTtl` in `web/src/lib/db.js` / `rooms.js` |
| Calls (P2P voice/video) | Implemented (Merged) | `web/src/lib/rooms.js:589,754` (`addStream` over Trystero/WebRTC); call UI in `views/chat.js`; STUN/TURN `web/src/net.js:54` |
| Push notifications | Implemented (Merged) | `web/src/lib/push.js`; `backend/src/push/push.service.ts` (web-push + FCM; APNs dependency). Vendor keys pending in Railway per `web/DEPLOY.md` |
| Translation (live, multi-provider) | Implemented (Merged) | `web/api/translate.js` (Google/Azure/Sarvam endpoints), `web/src/lib/translate.js`. Provider-abstraction **platform**: Implemented (Draft PR **#51**, `feat/translation-abstraction`) |
| Safety numbers + identity pinning | Implemented (Merged) | PRs #12/#14/#24–#28; `web/src/lib/crypto/{safety-number,identity-pin,identity-pin-store}.js`; QR via `qr-scan.js` |
| Send enforcement (A5) | Implemented (Merged — flag default OFF) | PR #31; `web/src/lib/crypto/identity-enforcement.js:77` (`enforcing = false`) |
| **Discovery coarse-location hotfix** | **Implemented (Merged)** | **PR #66**, merge `069905e`; `web/src/lib/discovery.js:49`; guard `web/test/discovery-coarse-broadcast.test.js`; [ADR-024](../adr/024-discovery-coarse-broadcast-hotfix.md) |
| Signing-key foundation + publication (executable rollback) | Implemented (Merged — DARK) | **PR #39**, merge `67bc221`; `web/src/lib/crypto/{signing-key-store,signing-key-publication}.js`, `backend/src/auth/signing-keys.*`. **merged DARK — activation pending a separate owner-authorised change** (`SIGNING_PUBLICATION_ENABLED = false`) |
| X3DH + prekeys | Implemented (Merged — DARK) | **PR #41**, merge `f9fe579`; `web/src/lib/crypto/x3dh.js`, `backend/src/auth/prekeys.*`, migration `20260801180000_x3dh_prekeys`. **merged DARK — activation pending a separate owner-authorised change** (behind `spotme.e2e3`) |
| Double Ratchet | Implemented (Merged — DARK) | **PR #42**, merge `288b8ca`; `web/src/lib/crypto/ratchet.js` (004b-oracle conformant, byte-for-byte). **merged DARK — activation pending a separate owner-authorised change** (behind `spotme.e2e3`) |
| Multi-device | Deferred | **#43 SKIPPED** pending the ADR-008 §BLOCKING multi-device safety-number decision (owner **NOT DECIDED**, 2026-08-03); branch `feat/multi-device` not merged |
| Camera suite CAM-1…4 | Implemented (Draft PR **#56/#58/#59/#55**, `feat/camera-engine` + stacked) | branches **frozen** by owner directive; dark flags |
| Discovery V2 map | Implemented (Draft PR **#60**, `feat/discovery-v2-map-foundation`) | dark, fence-tested; **rebase pending** after #66 (ADR-024) |
| Live Nearby Events | Implemented (Draft PR **#61**, `feat/live-nearby-events`, stacked on #60) | dark, fence-tested |
| Nearby Moments | **Implemented (Merged — DARK)** | Phase 5 chain **#97–#101** landed 2026-08-04 (`bd94cf2`…`ba50e38`); **ADR-028 Accepted**; `MomentsModule`/`MediaModule` NOT imported; see the Phase 5 rows below |
| AI Interactive Map (assistant) | **Implemented (Merged — DARK)** | Phase 6 chain **#103–#107** landed 2026-08-04 (`0470217`…`629a4f1`); deterministic-only (no AI SDK/model/endpoint); `AssistantModule` NOT imported, web-next surface NOT mounted; see the Phase 6 rows below |
| SpotMe Exchange | Planned | PRD draft in PR **#64** (`handbook/product/exchange/`), pending A5 ratification; **no code** |
| Live voice translation | Implemented (Draft PR **#49** scaffold + **#54** pipeline, `feat/live-voice-*`) | scaffolding; not on master |
| Adaptive transport + Bluetooth mesh | Implemented (Draft PR **#50**, `feat/adaptive-transport-scaffold`) | scaffolding; not on master |
| Centrifugo transport | Deferred | seam on master (`web/src/lib/transport/centrifugo-adapter.js`; backend `realtime.controller.ts:71` returns 503 unless configured); **no client dependency in any package.json**; `feature/centrifugo-transport` abandoned (49 behind). ADR-002 keeps the interface |
| Media platform (media-core contracts) | In Progress | branch `feat/media-core-contracts` pushed with **no open PR** (audit anomaly A1) — see `spotme/docs/handbook/DECISIONS.md` item 7 (owner decision sheet, on the PR #62 branch) |
| **Platform Phase 1A** — dev/CI compose (PG+PostGIS, Valkey; opt-in `search-benchmark` profile) + additive PostGIS migration | Implemented (Merged — DARK) | **PR #72**, merge `7df509a`; `spotme/docker-compose.dev.yml`, migration `20260803120000_enable_postgis` |
| **Platform Phase 1B** — `@spotme/contracts` shared TS domain types (boundary-fenced, declaration output) | Implemented (Merged — DARK) | **PR #73**, merge `aa97fce`; `spotme/packages/contracts` |
| **Platform Phase 1C** — BullMQ `{maintenance}` queue on ioredis (disabled without `REDIS_URL`; sanitized DLQ; manual smoke only) | Implemented (Merged — DARK) | **PR #74**, merge `7a8c124`; `backend/src/queue/` — `QueueModule` NOT imported by `AppModule` |
| **Platform Phase 1D** — search benchmark harness (reproducible manifest; recorded run) | Implemented (Merged — DARK) | **PR #75**, merge `69cbedd`; `spotme/packages/search-bench` |
| **Platform Phase 1E** — AI Gateway ports + deterministic baselines (fenced); ADR-026/027 | Implemented (Merged — DARK) | **PR #76**, merge `983712f`; `web/src/lib/ai/` — fence `ai-gateway-not-shipped` |
| **Platform Phase 1F** — `web-next` React strangler beachhead (inert; outside the Vercel root; isolation-fenced) | Implemented (Merged — DARK) | **PR #77**, merge `f59d0ec`; `spotme/web-next` |
| **Platform Phase 1G** — storage characterization tests + observability baseline (all legs no-op without env; redaction-fenced) | Implemented (Merged — DARK) | **PR #78**, merge `9147485`; `backend/src/observability/`, `test/storage-characterization.spec.ts` — `ObservabilityModule` NOT imported |
| Engineering Handbook (this document set) | Implemented (Draft PR **#62**, stack #63/#64/#65) | `docs/engineering-handbook-v1` → `docs/discovery-platform-architecture` |
| **Platform Phase 2A** — Discovery contracts v1 (branded `CoarsePublicLocation`, compile-time negative tests) + programme doc + DPAS ch. 14 threat model | **Implemented (Merged — DARK)** | **PR #80**, merge `093a598`; `packages/contracts/src/discovery.ts`, `docs/architecture/discovery-platform/14-PRIVACY-ABUSE-THREAT-MODEL.md` |
| **Platform Phase 2B** — dark `DiscoveryModule` (policy/errors/ranking/service/controller), PostGIS models + hand-written migration, people query engine on real PostGIS | **Implemented (Merged — DARK)** | **PR #81**, merge `0db5f81`; `backend/src/discovery/`, migration `20260803190000_discovery_postgis` — `DiscoveryModule` NOT imported by `AppModule` |
| **Platform Phase 2C** — SearchPort + zero-dependency Typesense adapter (timeout/breaker/ceiling/exact-handle pin; live typo/prefix verification) + place/directions ports | **Implemented (Merged — DARK)** | **PR #82**, merge `69db179`; `backend/src/discovery/search/`, `backend/src/discovery/places/` — unconfigured by default, no credentials exist |
| **Platform Phase 2D** — deterministic intent router, closed-registry transparent ranking engine (safety hard gate), realtime contract | **Implemented (Merged — DARK)** | **PR #83**, merge `ac2db6b`; `backend/src/discovery/discovery.intent.ts`, `discovery.ranking.engine.ts`, `realtime/` |
| **Platform Phase 2E** — web-next Discovery UI (pure prop-driven components, SVG map, 12-state banner) + framework-free application layer behind 5 ports, on-device coarsening boundary, privacy mutation battery | **Implemented (Merged — DARK)** | **PR #84**, merge `bb6f550`; `web-next/src/discovery/` — NOT deployed, fixture ports only |
| **Platform Phase 2F** — dark integration fences (13 assertions incl. build-artifact scan), performance benchmarks (1M profiles achieved, PostGIS + Typesense), dark instrumentation (closed metric registry, redacted correlation logging), DPAS ch. 15/16/17 + doc set + 12-lens adversarial-review repairs | **Implemented (Merged — DARK)** | **PR #85**, merge `ce5a605`; `backend/test/discovery-dark-fences.spec.ts`, `backend/test/discovery-benchmark.e2e-spec.ts`, `backend/src/discovery/discovery.observability.ts`, `docs/architecture/discovery-platform/17-ADVERSARIAL-REVIEW-PHASE-2.md` |
| **Platform Phase 3A** — Exchange versioned contracts (branded coarse location, 8-state lifecycle, closed match-signal registry, safety gate as a type property, opaque cursor, no payment/age/gender shapes) + policy + threat model (21 threats) | **Implemented (Merged — DARK)** | **PR #86**, merge `a793609`; `packages/contracts/src/exchange.ts`, `packages/contracts/test/exchange-negative.test.ts`, `docs/architecture/exchange-platform/01-CONTRACTS-POLICY-THREAT-MODEL.md` |
| **Platform Phase 3B** — dark `ExchangeModule` (lifecycle engine, ownership isolation, optimistic concurrency, idempotent create, signed depth-bounded keyset) + 5 additive PostGIS tables (business participation a dark seam, D4) + migration | **Implemented (Merged — DARK)** | **PR #87**, merge `8c414cc`; `backend/src/exchange/`, `backend/prisma/migrations/20260804120000_exchange_backend` — `ExchangeModule` NOT imported by `AppModule` |
| **Platform Phase 3C** — Exchange matching engine (closed signal registry, safety hard gate, no popularity/sponsored path) + sanitized search projection (allow-list, coordinate-token stripping incl. category, T-EX-19) + explainable ranking | **Implemented (Merged — DARK)** | **PR #88**, merge `dd7bde7`; `backend/src/exchange/exchange.matching.ts`, `exchange.search.ts` — search adapter unconfigured by default |
| **Platform Phase 3D** — inert web-next Exchange experience (composer, browse, functional Mine tab, explainable matches, consent-gated contact, no-payment disclaimer, A3 no age/gender, on-device coarsening boundary, privacy mutation battery) | **Implemented (Merged — DARK)** | **PR #89**, merge `b769e6b`; `web-next/src/exchange/` — fixtures only, `App.tsx` mounts only Discovery |
| **Platform Phase 3E** — Exchange dark integration fences (10, incl. web-next-entry + behavioral business-seam), privacy-safe instrumentation (closed metric/label registries), measured performance, runbooks + activation checklist, 13-lens adversarial-review repairs | **Implemented (Merged — DARK)** | **PR #90**, merge `7659d43`; `backend/test/exchange-dark-fences.spec.ts`, `backend/src/exchange/exchange.observability.ts`, `docs/architecture/exchange-platform/` |
| **Platform Phase 4A** — Live Nearby Events versioned contracts (`EventPublic`, time-derived `EventState` with source cancelled/postponed winning over the clock, mandatory `EventSource` provenance, optional branded 0..1 popularity, closed ranking-signal set, explainable dedup) + compile-time negative tests + threat model (fake events, lure-to-location, spam, provider poisoning) + Phase 4 programme table | **Implemented (Merged — DARK)** | **PR #92**, merge `b8f0372`; `packages/contracts/src/events.ts`, `packages/contracts/test/events-negative.test.ts`, `docs/architecture/events-platform/01-CONTRACTS-POLICY-THREAT-MODEL.md` |
| **Platform Phase 4B** — dark `EventsModule` (unimported) + additive PostGIS storage (Event/DedupDecision/SearchProjection; no user-origin column) + normalize boundary (venue coarsening, source-only popularity, C3 provenance, C5 time) + conservative cross-provider dedup (C4) + provider port (fixture/unavailable only) + sanitized projection + keyset browse | **Implemented (Merged — DARK)** | **PR #93**, merge `2e76944`; `backend/src/events/`, migration `20260804140000_events_backend` — `EventsModule` NOT imported by `AppModule` |
| **Platform Phase 4C** — transparent events ranking (closed registry; engagement throws; popularity last, tie-break only, unknown omitted — C2) + inert web-next Events surface (browse/detail/save/map, source attribution + provenance, coarsening boundary, honest states, A3) | **Implemented (Merged — DARK)** | **PR #94**, merge `c236044`; `backend/src/events/events.ranking.ts`, `web-next/src/events/` — fixtures only, `App.tsx` unchanged |
| **Platform Phase 4D** — Events dark integration fences (11, incl. web-next-entry + no-origin-column + behavioral venue-coarsening) + privacy-safe instrumentation (closed metric/label registries) + benchmark (50k achieved) + runbooks + activation checklist + docs | **Implemented (Merged — DARK)** | **PR #95**, merge `5949e4f`; `backend/test/events-dark-fences.spec.ts`, `backend/src/events/events.observability.ts`, `docs/architecture/events-platform/` |
| **Platform Phase 5A** — Nearby Moments versioned contracts (M5 four-tier visibility with `FeedVisibility` excluding private, M3 six-state stories, M4 flat comments + closed reactions, closed M2 ranking registry, no engagement counts on public shapes) + compile-time negatives + **ADR-028** data/privacy model (Proposed) + M6 threat model + Phase 5 programme | **Implemented (Merged — DARK)** | **PR #97**, merge `bd94cf2`; `packages/contracts/src/moments.ts`, `docs/adr/028-nearby-moments-data-privacy-model.md`, `docs/architecture/moments-platform/01-CONTRACTS-ADR-THREAT-MODEL.md` |
| **Platform Phase 5B** — dark media pipeline: M1 media ports on the existing `IStorageAdapter`, EXIF/GPS strip proven before persistence, M8 reserved queues (`{moment-media}`/`{story-expiry}`/`{feed-refresh}`/`{moderation}`) as contracts + fixture workers (inert without env), sha256 dedup + refCount cascade | **Implemented (Merged — DARK)** | **PR #98**, merge `c8d0c0d`; `backend/src/moment-media/`, migration `20260804200000_moment_media` — `MediaModule` NOT imported |
| **Platform Phase 5C** — dark MomentsModule: M5 four-tier visibility (private never in any feed SQL), three feeds chronological-first + M2 frozen registry (forbidden throws by name), M3 six-state stories + `{story-expiry}`, M4 flat comments + closed reactions (DB CHECK), two-way block in SQL, moderation machine + sanitized audit + child-safety priority lane, Disabled realtime | **Implemented (Merged — DARK)** | **PR #99**, merge `7865f8f`; `backend/src/moments/`, migration `20260804210000_moments_backend` — `MomentsModule` NOT imported |
| **Platform Phase 5D** — inert web-next Moments surface: two-step M5 location attach (plain-language explanation before any attach), untrusted URLs (no unfurl), closed reaction bar, flat-data comments, report/block/hide, no engagement counters, virtualized 3-mode feed, CameraPort interface-only | **Implemented (Merged — DARK)** | **PR #100**, merge `74e4d19`; `web-next/src/moments/` — App unchanged |
| **Platform Phase 5E** — Moments dark fences + full M9 battery (import-graph/dependency/ranking-invariant/storage-boundary/artifact scans, EXIF re-proof, private-never-in-projection), closed metrics, benchmark (100k achieved, honest linear-growth flag), runbooks (child-safety mandatory path), activation checklist (moderation staffing + legal review HARD) | **Implemented (Merged — DARK)** | **PR #101**, merge `ba50e38`; `backend/test/moments-dark-fences.spec.ts`, `docs/architecture/moments-platform/` |
| **Platform Phase 6A** — Assistant versioned contracts (X1 no-prose answer union with `NonEmptyArray` citations making uncited claims uncompilable, X2 closed six-member license class — scraped/unknown unrepresentable, X4 freshness with `CurrentStateCategory` gate, X5 rating-free review facets, X6 route origin boundary — precise device fix unassignable, X8 domain darkness registry) + compile-time negatives/positive control + threat model (T-AS-1..12) + Phase 6 programme | **Implemented (Merged — DARK)** | **PR #103**, merge `0470217`;  `packages/contracts/src/assistant.ts`, `packages/contracts/test/assistant-negative.test.ts`, `docs/architecture/assistant-platform/01-CONTRACTS-AND-THREAT-MODEL.md`, `docs/handbook/PLATFORM-PHASE-6-PROGRAMME.md` |
| **Platform Phase 6B** — dark AssistantModule composed ONLY of the X7 ports: live darkness registry (every domain implemented-dark → every live answer domain-not-active, X8), deterministic intent router + closed-template summary composer (no generation), evidence mint boundary (non-member license classes and forbidden fields THROW; X3 category match; X4 freshness gate), QueryHandle bounded-lifetime query privacy with closed-code errors (X9), closed sensitive-query classification + [PROPOSED] digit-free copy | **Implemented (Merged — DARK)** | **PR #104**, merge `75815a5`;  `backend/src/assistant/` — `AssistantModule` NOT imported by `AppModule`; `backend/test/assistant-{evidence,intent-sensitive,compose-service,privacy}.spec.ts` (49 green incl. F2-F4 regressions) |
| **Platform Phase 6C** — X5 review-intelligence engine (closed facet registry; mixed facets keep BOTH conflicting claims visible; no rating/stars/score key anywhere; facets without evidence are honest insufficient-evidence rows; confidence from diversity+freshness+density, not review count) + route phase-1 (X6: origin/destination QUANTIZED to the public grid — corrected by review repair F1: exact-grid throw falsely rejected legitimate and jittered coarse values; provider legs attributed passthrough citing CURRENT route-leg evidence, absent numbers stay null; straight-line fallback fixed-labeled with no duration field; community road-reports named as a future Moments seam only) | **Implemented (Merged — DARK)** | **PR #105**, merge `9276c87`;  `backend/src/assistant/assistant.review.ts`, `assistant.route.ts`; `backend/test/assistant-{review,route}.spec.ts` (25 green incl. F1/F4 regressions; assistant suite 74) |
| **Platform Phase 6D** — inert web-next conversational map surface: query bar with the honestly-disabled VoicePort mic seam, cited answer cards (citation labels from normalized records per X3, unresolved citations render as explicit failures, stale evidence carries an out-of-date disclosure), honest insufficient-evidence/domain-not-active/unavailable state cards, X5 review panel (mixed shows both sides, no stars/score anywhere), X6 route cards (attributed passthrough, verbatim straight-line label, no time on estimates), digit-free sensitive notices, coarsening boundary + privacy-mutation battery that also scans for query text (X9) | **Implemented (Merged — DARK)** | **PR #106**, merge `5cacb17`;  `web-next/src/assistant/` — App unchanged, fixtures only; `web-next/test/assistant-{controller,privacy-mutation,ui}.*` (21 green; web-next 105 + fence 6/6) |
| **Platform Phase 6E** — the X10 fence battery (module darkness, AI-SDK/model/endpoint/env-var scan, network-client scan, domain-internal-import scan, persistence/hash scan, rating scan, build-artifact scan, behavioral re-proofs; tamper-checked non-vacuous) + citation-integrity invariants (records frozen at mint, removing evidence invalidates dependent claims, no cross-envelope citation, no uncited source in an answer) + closed dark metrics (query text refused as a label by key name AND enum membership) + runbooks (provider outage, evidence-integrity incident, privacy incident, two-line dark rollback) + owner-gated activation checklist (licensing/spend, LLM decision through the AI Gateway, sensitive-copy ratification, per-domain activation) | **Implemented (Merged — DARK)** | **PR #107**, merge `629a4f1`;  `backend/test/assistant-dark-fences.spec.ts`, `assistant-citation-observability.spec.ts`, `backend/src/assistant/assistant.observability.ts`, `docs/architecture/assistant-platform/` (backend 146 green post-repair, contracts fence 6/6, web-next 105) |
| **ADR-035 slice 2** — Contacts · Notifications · Stories in React 19 (`packages/ui/{contacts,notifications,stories}`), each behind its own dark flag `spotme.ui.contacts` / `.notifications` / `.stories` (default OFF, read only through `uiFlag()`); legacy views stay in `ROUTES` with a one-line flag branch each; app-side port adapters reuse the legacy libs (db/lobby/reach/notify) so no persisted shape changes (§(g) tier 3 — only write is the legacy `notifSeenTs` key); parity + privacy tests (`apps/web/test/ui-slice2-parity.test.js`, `packages/ui/test/{contacts,notifications,stories}-ui.test.tsx`) | **Implemented (Draft PR — DARK)** | branch `feat/slice-2-small-surfaces` (base `feat/slice-1-exchange-ui`) |
| **ADR-035 slice 3** — Groups (list · new · manage) in React 19: `packages/ui/groups/` (ports/screens/island, tokens-only CSS), `lib/group-perms.js` reused via an injected port, flag `spotme.ui.groups` default OFF read only in `apps/web/src/views/groups-island.js`, legacy views unchanged except the one-line flag branch, parity + permission-gating batteries | **Implemented (PR open — DARK)** | branch `feat/slice-3-groups` (base `feat/slice-1-exchange-ui`); `apps/web/test/groups-flag-parity.test.js`, `packages/ui/test/groups-{ui,perms-gating}.test.*` |
| **ADR-035 slice 4** — Profile · Settings in React 19 (`packages/ui/profile`) behind dark flag `spotme.ui.profile` (default OFF, read only through `uiFlag()`); legacy view stays in `ROUTES` with a one-line flag branch; media/crop/photoedit port boundary enforced — photo upload + crop, DiceBear AI avatars, voice cloning, and the username registry live app-side in `island-adapters-profile.js` + `voice-clone-sheet.js`, never in the package; §(g) tier 3 held (adapter writes only the exact `setProfile`/`setSettings` keys the legacy view writes, pinned by test); parity + UI tests (`apps/web/test/ui-slice4-parity.test.js`, `packages/ui/test/profile-ui.test.tsx`) | **Implemented (Draft PR — DARK)** | branch `feat/slice-4-profile` (base `feat/slice-2-small-surfaces`) |
| **ADR-035 slice 6** — Inbox in React 19: `packages/ui/inbox/` (ports/shell/sheets, tokens-only CSS) behind `spotme.ui.inbox` (default OFF, read only in `apps/web/src/views/inbox-island.js`); legacy `views/inbox.js` unchanged except the one-line flag branch; `lib/rooms.js` (the chat engine) reused via the injected port — realtime crosses the port as a db/lobby subscription with display-ready rows, the only spelled shape (createGroup convo) is the legacy shape verbatim; parity + UI batteries (`apps/web/test/inbox-flag-parity.test.js`, `packages/ui/test/inbox-ui.test.tsx`) | **Implemented (PR open — DARK)** | branch `feat/slice-6-inbox` (base `feat/slice-2-small-surfaces`) |
| **ADR-035 final slice, session 1 of N** — Chat characterization tests FIRST (`apps/web/test/chat-characterization.test.js`: 37 assertions pinning send/receive ordering, preview grammar, reply/translation envelopes, monotonic read receipts, typing, edit/delete/reactions, disappearing-timer control, and the view's Read/Sent/Not-delivered + Today-divider derivations), then `packages/ui/chat` — message list + composer core ONLY (windowed list, text send, day dividers, status ticks, typing line, failed-text Retry) behind `spotme.ui.chat` (default OFF, read only in NEW `views/chat-island.js`); legacy `views/chat.js` carries the one-line branch and is otherwise untouched; the adapter reuses `lib/rooms.js` verbatim (zero persisted-shape change, §(g)); media, sheets, voice, calls, translation UI and ALL crypto-facing UI stay legacy-only — later sessions | **Implemented (Draft PR — DARK, partial surface)** | branch `feat/chat-react-s1` (base `feat/slice-2-small-surfaces`); `packages/ui/test/chat-ui.test.tsx` (14), `apps/web/test/chat-flag-parity.test.js` (7) |
| **ADR-035 final slice, session 2 of N** — media rows in the React list (photo, view-once locked tile rendering the sender-made mask ONLY, voice playback row, file row, location rows), attach sheet (photo/private-photo/file/voice/location → port callbacks), long-press message sheet (reply/edit/delete/react/copy), reaction picker + aggregated chips, reply composition (quote bar; the send carries the same `replyTo` envelope the characterization suite pins). Adapter split: `views/chat-island-port.js` (ChatPort over the same `lib/rooms.js` calls — zero persisted-shape change) + `views/chat-island-media.js` (all raw File/canvas/MediaRecorder/geolocation/clipboard APIs); flag still read only in `chat-island.js`. View-once burn-before-reveal + never-persists pinned by a dedicated suite. Still legacy-only: calls, translation/transliteration composer modes, live location sharing UI, in-chat group management, crypto-facing UI | **Implemented (Draft PR — DARK, partial surface)** | branch `feat/chat-react-s2` (base `feat/chat-react-s1`); `packages/ui/test/chat-media-ui.test.tsx` (17), `apps/web/test/chat-viewonce-react.test.js` (13), `chat-flag-parity.test.js` extended (8) |
| **ADR-035 final slice, session 3 of N** — translation + transliteration composer modes in the React chat: header 文A/🌐 switches + language chips (same convo keys the legacy header persists: `composeMode`/`langTo`/`xlit`/`xlitLang`), live conversion preview (instant on-device transliteration + debounced remote upgrade; translate hero line + Detected sub), translate-before-send shipping the legacy `tr:{lang,text}` envelope with a 4s all-engines-down fallback to the raw draft, translit-as-reading-aid (never rewrites outgoing text), incoming auto-translation lines (sender-shipped `tr` instant path + detection path). Engines (`lib/translate.js`, `spotme-core/core/translit.js`) driven UNCHANGED through NEW `views/chat-island-lang.js` (injected deps); the package sees strings only. Polish: Forward/Share sheet items (legacy canForward gate + forwardTo clone), swipe-to-reply, double-tap ❤️, real video playback row, decorative voice bars. Still legacy-only: calls, live location sharing UI, whole-thread transliteration reading lines, "Viewing now" ring, in-chat group management, crypto-facing UI | **Implemented (Draft PR — DARK, partial surface)** | branch `feat/chat-react-s3` (base `feat/chat-react-s2`); `packages/ui/test/chat-lang-ui.test.tsx` (11), `apps/web/test/chat-lang-react.test.js` (8), `chat-flag-parity.test.js` fence extended to `chat-island-lang.js` |
| **ADR-035 final slice, session 4 of N** — crypto-facing surfaces. NEW `packages/ui/verify` (VerifyShell: loading→ready phases, honest e2e_v1 copy, formatted safety-number digits, QR via port-provided data: URL, CHANGED-key Accept/Keep with no dismiss, scan verdicts, enforcement-aware footer) behind `spotme.ui.verify` (default OFF, read only in NEW `views/verify-island.js`; legacy `views/verify.js` one-line branch). Adapter reuses the legacy crypto engine calls verbatim; crypto values cross the port as display strings only; the package imports no crypto module (packages/ui fence extended to `verify`). Chat crypto rows via `ChatSnapshot.security` (encryption claim + Verify entry point, undecryptable / identity-key warning) shaped by NEW `views/chat-island-crypto.js` (reads `identityStatus()` only; legacy sentences verbatim), undecryptable state machine (wrong-key upgrades no-key, first opened frame clears) in `chat-island-port.js`. ADR-008 §12 untouched; `signing-not-shipped` + `e2e-v3-not-shipped` fences green. Still legacy-only: calls, live location sharing UI, whole-thread transliteration reading lines, "Viewing now" ring, in-chat group management | **Implemented (Draft PR — DARK, partial surface)** | branch `feat/chat-react-s4` (base `feat/chat-react-s3`); `packages/ui/test/verify-ui.test.tsx` (10), `verify-privacy-mutation.test.tsx` (5), `chat-security-ui.test.tsx` (5), `apps/web/test/verify-flag-parity.test.js` (9), `chat-crypto-react.test.js` (6) |
| **ADR-035 slice 5** — Discovery in React 19 (`packages/ui/discovery-live`, NOT the dark Phase-2 `packages/ui/discovery`) behind `spotme.ui.discovery` (default OFF, read only in `views/discovery-island.js`); pinned to today's live lobby — no Phase 2 backend, no Typesense; `lib/discovery.js` (ADR-024 fence-covered) reused via ports, unmodified; no coordinate crosses the port (labels + relative metre offsets only); `test/discovery-coarse-broadcast.test.js` extended to assert BOTH implementations in one run; parity + privacy tests (`apps/web/test/discovery-flag-parity.test.js`, `packages/ui/test/discovery-live-{ui,privacy-mutation}.test.*`); live Google map stays legacy-only (package fence forbids external scripts) | **Implemented (Draft PR — DARK)** | branch `feat/slice-5-discovery` (base `feat/slice-2-small-surfaces`) |

## Platform Phase 1 — landed decisions (2026-08-03, delegated approval)

- **ADR-026 Accepted** (realtime split-plane) and **ADR-027 Accepted**
  (mobile-native boundary) — statuses flipped in the ADRs and index (PR #76).
- **Search: Typesense selected** as the Phase 2 target from the committed
  benchmark (~12× faster warm p50/p95; Meilisearch documented fallback).
  **Not yet wired. Not yet active.** Mandatory production-hardware re-benchmark
  before wiring (tech-stack §14; `handbook/DECISIONS.md`).
- Everything in Phase 1 landed **DARK**: both crypto flags false throughout,
  no feature activation, no flag changes, no user-visible behaviour change.

## Retired

| Item | Replaced by |
|---|---|
| `.handoff/NEXT-SESSION.md` + `SESSION-*.md` pickup mechanism | This handbook + [00-BOOTSTRAP](00-BOOTSTRAP.md); RETIRED banner in the file |
| A1–A7 identity labels (where they conflict with Roadmap V2) | Roadmap V2 numbering + `14-ROADMAP-V1-TO-V2-MAPPING.md` |
| V1 migration plan (`MIGRATION-PLAN-V1.md`) | Roadmap V2 (historical; stricter V1 gates still bind per V2 Appendix B) |
| 2026-07-25 precise-broadcast decision | [ADR-024](../adr/024-discovery-coarse-broadcast-hotfix.md) (PR #66) |

**Needs owner clarification (not Retired):** the Vercel `/api/*` functions were
previously listed Retired per `09-TECH-STACK.md` ("vestigial"), but the
2026-08-03 audit found live vendor endpoints in them and `web/DEPLOY.md` notes
the same files are staged into the Railway image — treat as live until the
owner rules.

## Product framing

The three flagship pillars, the product loop, and the fixed Discovery order
live in the [product authority](product/README.md) and
[ADR-021](../adr/021-spotme-unified-product-ecosystem.md) /
[ADR-022](../adr/022-discovery-execution-sequence.md).
This page stays authoritative for *what is actually built*.
