{
  "name": "spotme-e2e",
  "private": true,
  "type": "module",
  "description": "End-to-end tests. Separate package so neither web nor backend gains a Playwright dependency.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
