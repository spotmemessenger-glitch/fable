# Full session log — 2026-07-29

Chronological record of everything done. `NEXT-SESSION.md` is the brief to act
on; this is the evidence behind it. Total cost ~$367.

---

## Phase 1 — Plugin verification (user: "registered isn't the same as working")

Live-tested seven queued MCP servers instead of trusting the registry:

| Server | Result |
|---|---|
| github | working, auth'd as `spotmemessenger-glitch` |
| desktop-commander-local | working |
| claude-video-vision | responding |
| openhands (MCP) | responding |
| claude-mem | connected but **0 corpora** — empty shell |
| graphify | connected but **`graph.json not found`** |
| getlayers | **not authorized** — OAuth, impossible non-interactively |

User was right: 2 of 7 were registered-but-useless, 1 dead.

## Phase 2 — 9router + OmniRoute

- Installed `9router` 0.5.40. **Did not start it** — collides with OmniRoute on
  `:20128` and is the weaker sibling (v0.5.40/40 providers vs v3.8.48/160).
- OmniRoute was never down: `omniroute chat` returned `ROUTER_OK` in 3.9 s via
  the `big-pickle` combo.
- Found 5 providers already configured (gemini 429-limited, codex model error,
  kimi-coding **already connected**, opencode untested, openrouter ok).
- **Cerebras could not be added: `CEREBRAS_API_KEY` is empty** (length 0; live
  API returns `Not authenticated`). Dashboard is password-gated; did not enter
  credentials. Backed up `storage.sqlite` before touching anything — ultimately
  did not need to write to it.

## Phase 3 — Repairing broken setups

- **Firecrawl** — whole 7-container stack was stopped. Restarted; `:3002`
  serves and a real Hacker News scrape returned markdown. (`foundationdb-init`
  "Database already exists" is benign.)
- **claude-mem** — built a 46-observation `fable` corpus, primed it, confirmed
  it answers real queries.
- **graphify** — first attempt was WRONG and was reported as such: unscoped at
  fable root it produced 173 MB / 134,090 nodes / **0 edges**. Rebuilt scoped to
  `spotme/`: 1627 nodes, 2823 edges, 113 communities, 98% EXTRACTED. MCP now
  returns real symbols (`AuthenticatedPrincipal`, `PrismaService`, `P2P`).
- **OpenHands** upgraded 1.5.2 → **v1.6.1**, `--restart unless-stopped` added,
  old container kept as `openhands-agent-canvas-v152-backup`. `/canvas/` → 200.
- **AutoGen** — already at 0.7.5, which is latest. Nothing to do.
- **MetaGPT** — diagnosed rather than retried: **PyPI `metagpt` is v0.1**, an
  ancient stub, which is why pip backtracked through `googleapis-common-protos`
  for 10 minutes. Installed v1.0.0 from GitHub into a 3.11 venv.

## Phase 4 — Skills (118 installed, zero collisions)

| Repo | Count |
|---|---|
| google/skills | 93 |
| android/skills | 20 |
| haidrrrry/compose-kotlin-agent-skills | 4 |
| anhvt52/jetpack-compose-skills | 1 |

No `android` CLI exists here, so installation was a directory copy of every
`SKILL.md` folder. Every name was checked against existing skills first.

**Daily updater built** (CronCreate was rejected — session-only, 7-day expiry):
`fable/scripts/update-android-skills.sh` + Windows Scheduled Task
`AndroidSkillsDailyUpdate` at 09:07. Test-fired: `LastTaskResult: 0`.

## Phase 5 — Desktop-automation stack

Audited 70 listed repos, found ~50 were runtimes/databases, not agent skills.

Installed and verified: **OmniParser** (0.17 s warm / 222 elements — after
fixing CPU-only torch and the broken v3 weights), **SAM 2** (fixed: no
checkpoints shipped; `from_pretrained('facebook/sam2.1-hiera-tiny')`),
EasyOCR (166 regions off the live desktop), OpenCV 5.0.0, torch cu126.
Also pynput/watchdog/GitPython/fsspec on ybot's 3.14; Coqui TTS, whisper,
piper, kokoro on the voice venv; puppeteer/selenium/stagehand; PowerShell 7.6.4,
PSScriptAnalyzer 1.25.0, PowerToys, Windows Terminal, Trivy 0.72.0;
openai-agents, pydantic-ai, llama-index, chromadb, litellm.

**Verified VRAM co-residency: OmniParser + SAM 2 = 0.75 GB of 4 GB.**

## Phase 6 — 3D stack

CAD proven end-to-end: CadQuery solid (vol 464.7, 7 faces) **exported STEP via
the OCCT B-rep kernel**. Plus trimesh (5120→200 faces, watertight),
Open3D, usd-core (full `UsdShade` material bind + save), MaterialX 1.39.5 with
`open_pbr_surface`, pybullet, **Warp on `cuda:0`**, pymeshlab, ifcopenshell.
Blender 5.2.0 LTS headless bpy verified. `blender-mcp` 1.6.5 registered with
Claude Code, `✔ Connected`. Godot 4.7.1, three/babylon/playcanvas.

Caught a hidden breakage: **trimesh decimation was broken** (missing
`fast-simplification`) — import succeeded, the actual call failed.

## Phase 7 — 8 agents + 8 3D skills authored

Agents: ceo, planner, vision, desktop-operator, browser-operator, memory,
recovery, optimization. Coding/QA/Security/Research NOT built — 89 existing
agents cover them; duplicating would create routing ambiguity.

3D skills: blender-automation, cad-parametric-modeling, mesh-optimization,
usd-gltf-pipeline, pbr-materials-openpbr, game-ready-3d-assets,
physics-simulation-3d, web3d-development. **Every code sample was executed
before being written into the skill.**

## Phase 8 — ybot voice subsystem (1059 lines, 9 modules)

Read ybot's real code first (`main.py`, `config.py`, module sizes) rather than
assuming interfaces. Discovered the **3.14 vs 3.11 split**, which forced the
microservice architecture the user had asked for anyway.

All nine modules pass their `demo()` self-check:
`style, mic, vad, memory, intent, orchestrator, providers, service, bridge`.
`bridge.py` asserts torch/numpy/sounddevice never leak into 3.14 — and passes
on the real 3.14 interpreter.

Deliberately did NOT modify `Operator`, `Settings`, or `main.py`.

---

## Things that failed, and why (kept for honesty)

- Tesseract, FreeCAD, MeshLab — UAC elevation, `0x800704c7`, three times.
- COLMAP, Meshroom — not in winget at all.
- `llama-cpp-python`, `open-webui` — need MSVC to build.
- `pyassimp` — installs, missing native lib, unusable.
- Kaolin — skipped on purpose; would downgrade torch and break the vision stack.
- Bash heredocs broke twice on quoting; switched to the Write tool.
- Ran an EasyOCR check without redirecting output — a progress bar burned a
  large amount of context. Redirect model/pip output from the start.
