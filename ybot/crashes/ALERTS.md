# Ybot crash alerts — active board

Claude: on seeing this file, triage every [OPEN] line (bundle in inbox/<id>/), then mark it [FIXED]/[DIAGNOSED].

- [DIAGNOSED] 2026-07-24T09:51:04Z `20260724-152104-cded74afae` RuntimeError: SYNTHETIC TEST CRASH: crash-bridge end-to-end verification, not a real bug (count 1, triage spawned) — reported crash was synthetic (raised from a `python -c` one-liner, no ybot frame) — no defect there. BUT the triage hit a real bridge bug: `triage.log` was opened INSIDE the bundle, so on Windows the session's own open handle blocked step 6 and no bundle could ever leave `inbox/`. FIXED in `ybot/crash_reporter.py` (logs now go to `crashes/logs/<id>.log`, parent handle closed). Restart Ybot if running. See `triaged/20260724-152104-cded74afae/triage.md`.
- [DIAGNOSED] 2026-07-29T19:03:46Z `20260730-003346-f538eecb49` AuthenticationError: 401 invalid x-api-key from `run.py` (count 1, triage spawned) — the credential in use at that moment was rejected outright, so no request could succeed. **No longer reproduces:** on 2026-07-30 the same path (`SETTINGS.api_key` → Anthropic client) authenticated successfully against `claude-sonnet-5` and `claude-opus-4-8`. The key was replaced between the two runs; nothing in ybot was changed to fix it, hence DIAGNOSED rather than FIXED. Reopen if a 401 returns — that would mean the key is being read from a different .env than the one that works (ybot/.env takes precedence over fable/.env, which `config.py` loads with `override=False`).
- [FIXED] 2026-07-30 BadRequestError: 400 `'claude-sonnet-4-5-20250929' does not support tool types: computer_20251124` — the computer-use tool version was a hardcoded constant while the model was configurable, so a model predating the newer tool failed on EVERY step. Tool version now follows the model (`config.computer_tool_for`), with a one-shot retry on the opposite version if a future model is not in the list. Two stale pins in `ybot/.env` also fixed: `claude-opus-4-1-20250805` 404s on this key, so smart mode could never have run.
